{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the FreeCut Local manager window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
